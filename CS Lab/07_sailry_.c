#include<stdio.h>
 
int main()
{
    int sailry, gross;
    if (sailry<=5000)
    {
        gross=sailry*10/9;
        printf("gross sailry isw %d", gross);
    }
    if (sailry<=20000 )
    {
        gross=sailry*10/9;
        printf("gross sailry isw %d", gross);
    }
    if (sailry>20000)
    {
        gross=sailry*10/9;
        printf("gross sailry isw %d", gross);
    }
    return 0;
}